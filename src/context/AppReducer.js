import React from "react";

const AppReducer = (state, { type, payload }) => {
  switch (type) {
    case "ADD_USER":
     

    case value:
      break;

    case value:
      break;

    case value:
      break;

    default:
      break;
  }
};

export default AppReducer;
